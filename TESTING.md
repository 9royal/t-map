# T map v1.04.6 測試紀錄

## 本輪目標：準星進入區域後持續提示，外島使用相同接觸規則

本版只修改 `v1.04-testing` 的放大鏡接觸判定。

### 自動測試

- Node 單元測試：**24 / 24 通過**。
- `app.js`、`puzzle-hints.js` 通過 `node --check`。
- 新增／修正測試：
  - `crosshairCircleContact()`：中央點已進入正確區域時，即使離邊界很遠仍維持接觸。
  - 圓圈中央尚未進入時，邊界距離 ≤ 約 3.4px 才算接觸；24px 舊容許半徑不得誤吸附。
  - 澎湖、金門、連江使用同一接觸規則。
  - `magnifierContact()`：提示 ON 時接觸會亮且可放置；提示 OFF 時不亮，但同一接觸仍可放置。
  - `magnifierSourceRadius(8, 2.35)` 約為 3.4px，對應畫面上 16px 外徑的準星圓圈。

### 實機 Preview 驗收

1. 使用 `v1.04-testing` Cloudflare Preview。
2. 手機開啟「放大鏡 ON＋正確提示 ON」。
3. 將準星小圓圈從正確行政區外慢慢移入：**邊緣剛碰到時應亮黃；整個小圈進入正確區域後仍必須持續亮黃，不得消失**。
4. 在正確區域內放開：應成功吸附。
5. 讓小圓圈完全離開正確區域後放開：不得吸附。
6. 分別測試澎湖縣、金門縣、連江縣放大框：準星邊緣碰到或中央點進入正確外島輪廓時都應亮黃；放開應成功吸附。
7. 關閉正確提示後重測：不亮黃，但相同接觸條件仍可吸附。
8. 關閉放大鏡後確認原本手機智慧容許區仍正常。

## 建置檢查與限制

- 以臨時測試替身執行 `npm test → npm run build → npm run verify`：通過，並確認 22 縣市／368 鄉鎮市區數量檢查及 v1.04.6 版本一致性。
- 上述 build smoke 使用測試替身，不等同於真實 npm 套件建置；正式發布仍以 Cloudflare Preview 的真實 `npm install → build → verify` 為最後建置驗收。
