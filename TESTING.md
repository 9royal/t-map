# T map v1.04.4 測試紀錄

## 本輪目標：放大鏡中央準星判定

本版只調整 v1.04-testing 的放大鏡「正確提示亮色」觸發來源，不改動既有拖曳、智慧吸附、頁面捲動、台語朗讀與底圖提示分流。

### 自動測試

- Node 單元測試：19 / 19 通過。
- 新增 `magnifierHintName()` 測試：
  - 中央準星落在目前拼圖的正確行政區＋提示 ON → 回傳該行政區並在放大鏡 clone 高亮。
  - 中央準星落在錯誤行政區 → 不高亮，即使正確行政區位於既有 proximity／智慧容許半徑內。
  - 中央準星沒有落在任何行政區 → 不高亮。
  - 提示 OFF 或該區已完成 → 不高亮。
- `app.js`、`puzzle-hints.js` 皆通過 `node --check`。

### 實機 Preview 驗收

1. 使用 `v1.04-testing` Cloudflare Preview。
2. 手機開啟「放大鏡 ON＋正確提示 ON」。
3. 拖曳一塊行政區，讓拼圖名稱／文字靠近正確區域，但讓放大鏡中央準星仍在錯誤區域：**不得亮黃**。
4. 再移動到中央準星真正落入正確行政區：**只有放大鏡內該區亮黃＋發光**。
5. 底圖只保留一般碰觸效果，不同步顯示正確答案亮色。
6. 放開後仍沿用既有小行政區智慧容許區；因此「提示亮起門檻」與「最終可吸附容許度」刻意分離。

## 建置限制

本執行環境未重新下載 npm Registry 套件，因此正式發佈仍以 Cloudflare Preview 的真實 `npm install → build → verify` 為最後建置驗收。
