# T map v1.04 測試紀錄

## 已完成的自動檢查

- JavaScript 語法檢查：`app.js`、`puzzle-hints.js`、`mobile-map.js`、build / verify 腳本。
- Node.js 單元測試：10 項通過。
  - 提示 ON/OFF 與錯誤碰觸。
  - 已完成區不可重複放置。
  - 實際路徑距離與邊界附近容許值。
  - viewBox 解析。
  - 以焦點縮放、最大約 4 倍限制。
  - 平移不得超出原始 viewBox。
  - 是否處於放大狀態判定。
- Headless Chromium 響應式樣式煙霧測試：390px 時底部抽屜為 fixed、手機地圖工具顯示、收合拼圖允許橫向捲動；800px 時恢復一般版面且手機工具隱藏。
- 建置腳本煙霧測試：使用不隨發行包提供的本地測試替身，驗證 `build.mjs` 能建立 dist，`verify.mjs` 能通過 22／368 數量、部署檔、外部 CDN 與 v1.04 版本一致性檢查。此項只驗證建置流程，不等於真實圖資與函式庫端對端驗收。
- 靜態版本一致性與 ZIP 結構會在封裝前再次檢查。

## 正式發布前仍需人工裝置驗收

本執行環境不等於實際 iOS Safari／Android Chrome 觸控裝置。嘗試 `npm install` 時因本環境無法取得 npm Registry 而逾時，因此未在此處完成 Cloudflare 真實 npm 依賴建置。因此 v1.04 正式合併 main 前，請先使用 `v1.04-testing` Preview 測試：

1. 360×800、390×844、430×932 手機 viewport。
2. 拼圖片抽屜收合、展開與選取後自動收合。
3. 先點拼圖再點地圖，小行政區邊界外 24～30 px 的容許區是否自然。
4. 錯誤區附近仍應判錯，不應吸到正確答案。
5. 拖曳放大鏡位置、文字與主地圖提示色同步。
6. 正確位置提示 OFF 不發光；ON 才發光。
7. ＋／－／重設、雙指縮放、放大後單指平移。
8. 旋轉手機與從手機寬度切回平板寬度後，抽屜與放大鏡不應殘留。
9. 澎湖、金門、連江放大框仍可點選／拖放。
10. 完成進度、重新整理、深淺色、語音與音效正常。
