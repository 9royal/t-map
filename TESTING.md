# T map v1.04.5 測試紀錄

## 本輪目標：準星小圓圈同時控制提示與吸附

本版只修改 `v1.04-testing` 的放大鏡接觸判定。

### 自動測試

- Node 單元測試：**21 / 21 通過**。
- `app.js`、`puzzle-hints.js` 通過 `node --check`。
- 新增／修正測試：
  - `magnifierContact()`：準星圓圈碰觸正確區域時，提示 ON 會亮且可放置。
  - 提示 OFF 時不亮，但同一接觸仍可放置。
  - 未接觸正確區域時不可吸附。
  - `magnifierSourceRadius(8, 2.35)` 約為 3.4px，對應畫面上 16px 外徑的準星圓圈。

### 實機 Preview 驗收

1. 使用 `v1.04-testing` Cloudflare Preview。
2. 手機開啟「放大鏡 ON＋正確提示 ON」。
3. 將中央準星小圓圈慢慢靠近正確行政區；**圓圈邊緣剛碰到正確輪廓時，放大鏡內應開始亮黃＋發光**。
4. 讓圓圈與正確區域完全分離：黃色提示應立即消失。
5. 在「剛好接觸」狀態放開：應成功吸附。
6. 在圓圈未接觸正確區域時放開：不得因 24／30px 舊容許半徑而吸附。
7. 關閉正確提示後重測：不亮黃，但接觸正確區域後放開仍應成功。
8. 關閉放大鏡後確認原本手機智慧容許區仍正常。

## 建置檢查與限制

- 以臨時測試替身執行 `npm test → npm run build → npm run verify`：通過，並確認 22 縣市／368 鄉鎮市區數量檢查及 v1.04.5 版本一致性。
- 上述 build smoke 使用測試替身，不等同於真實 npm 套件建置；正式發布仍以 Cloudflare Preview 的真實 `npm install → build → verify` 為最後建置驗收。
